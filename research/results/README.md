# Results

Not yet measured. No run of the agent against the competition harness has been performed.

When runs exist, this directory holds, per frozen run:
- the `manifest.json` (variant, git SHA, archive SHA-256);
- `summary.json`;
- a link to the raw `records.jsonl`.

Negative and aborted runs are kept too.
